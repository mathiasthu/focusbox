# Brand source (derived favicons/icons/og are committed under public/)
logos/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local-only deploy target config (host/port/path) — never commit
.deploy-web.env

# Local-only ops script — keeps infra shape (paths/SSH/Apache) out of the public repo.
# The fillable template stays public as .deploy-web.env.example.
deploy-web.sh

# Local-only project docs / agent context (not for the public repo)
CLAUDE.md
HANDOFF.md
MONETIZATION-IDEAS.md
docs/cloud-sync-design.md
docs/superpowers/

# Editor directories and files
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.superpowers/

# Playwright MCP verification artifacts (snapshots/console logs/screenshots)
.playwright-mcp/
