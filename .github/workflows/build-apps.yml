name: Build apps

# Builds the installers in the cloud (no local toolchain needed) and signs the
# auto-update artifacts.
#
# - "Run workflow" button (Actions tab) → builds both installers and uploads each as a
#   downloadable artifact on the run page (no Release created).
# - Pushing a version tag like v0.2.2 → publishes a public GitHub Release with the
#   Windows (.exe) + macOS (.dmg) installers AND the signed updater artifacts +
#   `latest.json` that the in-app auto-updater reads.
#
# Requires repo secret TAURI_SIGNING_PRIVATE_KEY (the updater private key). The key has
# no password, so TAURI_SIGNING_PRIVATE_KEY_PASSWORD is an empty string below.

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  # Tests and the CSP gate run BEFORE anything is signed, and they run without any
  # secrets. Neither was wired into CI before: `npm run build` doesn't call check:csp,
  # and the gate's only caller was deploy-web.sh — which is gitignored, so the control
  # existed on exactly one machine.
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20

      - name: Install frontend dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Build frontend
        run: npm run build

      - name: CSP gate
        run: npm run check:csp

      # The installers consume this instead of rebuilding, so `vite build` — and the
      # ~500 npm packages that run during it — never execute in a job that holds the
      # updater signing key.
      - name: Upload frontend build
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: frontend-dist
          path: dist
          if-no-files-found: error
          retention-days: 1

  build:
    needs: checks
    permissions:
      contents: write # needed to create the Release
    # Updater signing secrets live ONLY in the `release` Environment (required reviewers),
    # so every signed build pauses for explicit approval — the key can't be used by an
    # unattended tag push. NOTE: this also means manual "Run workflow" builds wait for
    # your approval (the build needs the signing key, which is gated here). (M3 hardening.)
    environment: release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            args: "--bundles nsis"
            artifact_glob: src-tauri/target/release/bundle/nsis/*-setup.exe
            artifact_name: focusbox-windows-installer
          - os: macos-latest
            # `app` is required so createUpdaterArtifacts emits the macOS .app.tar.gz
            # updater bundle; `dmg` is the human download.
            args: "--bundles app,dmg"
            artifact_glob: src-tauri/target/release/bundle/dmg/*.dmg
            artifact_name: focusbox-macos-dmg
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2.9.1
        with:
          workspaces: src-tauri

      - name: Download frontend build
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: frontend-dist
          path: dist

      # --ignore-scripts: the only thing needed from npm in this job is the Tauri CLI
      # binary. Nothing here should execute package install hooks, and this job is the one
      # that holds the signing key. (Residual exposure, unavoidable: `tauri build` runs
      # cargo, so the ~560 crates' build.rs still execute with the key in the environment.
      # The larger point stands either way — for the same adversary, poisoning dist/ so the
      # legitimate build signs it needs no key theft at all.)
      - name: Install Tauri CLI
        run: npm ci --ignore-scripts

      - name: Build (+ publish Release on tag) with signed updater artifacts
        uses: tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5 # v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Signing secrets come from the `release` Environment (required reviewers).
          # The key is still passwordless, so the password resolves to "" until a key
          # rotation adds a TAURI_SIGNING_PRIVATE_KEY_PASSWORD Environment secret — at
          # which point this picks it up with no further edit. (See HANDOFF / security review.)
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          # Only release on a tag push; a manual run just builds (tagName empty → no release).
          tagName: ${{ github.ref_type == 'tag' && github.ref_name || '' }}
          releaseName: "Focusbox ${{ github.ref_name }}"
          releaseBody: |
            Focusbox — minimal countdown timer + tasks + notes.

            **Windows 11:** download the `*-setup.exe` and run it. SmartScreen may
            warn the first time → **More info → Run anyway** (unsigned, not unsafe).
            WebView2 is already built into Windows 11.

            **macOS (Apple Silicon):** download the `.dmg`, drag Focusbox to
            Applications. The app is unsigned, so the first launch needs a
            right-click → **Open** (or run `xattr -cr /Applications/Focusbox.app`).

            Once installed, Focusbox keeps itself up to date — it checks on launch and
            offers a one-click "restart to update".
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
          # tauri.ci.conf.json blanks beforeBuildCommand so the bundler uses the dist/
          # artifact built in `checks` rather than re-running vite here.
          args: ${{ matrix.args }} --config tauri.ci.conf.json

      - name: Upload installer artifact (for manual runs)
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_glob }}
          if-no-files-found: error
