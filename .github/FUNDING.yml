# Adds a "Sponsor" button to the GitHub repo.
# Replace the URL with your Stripe payment link (keep it in sync with
# src/lib/config.ts and the README).
custom: ["https://donate.stripe.com/REPLACE_WITH_YOUR_LINK"]
