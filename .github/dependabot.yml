version: 2
updates:
  # Keep SHA-pinned GitHub Actions current via automated PRs.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  # Keep npm dependencies current via automated PRs.
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
